{
  "name": "@converge/core",
  "version": "0.1.0",
  "private": true,
  "description": "Orchestration engine, convergence loop, agent management, and task decomposition",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@converge/shared": "workspace:*",
    "@converge/git": "workspace:*",
    "@converge/vision": "workspace:*",
    "@converge/bt-bridge": "workspace:*"
  }
}
