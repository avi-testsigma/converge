{
  "name": "@converge/vision",
  "version": "0.1.0",
  "private": true,
  "description": "Gemini Nano Banana Pro integration for mockup generation and visual comparison",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@converge/shared": "workspace:*",
    "@google/generative-ai": "^0.21.0"
  }
}
